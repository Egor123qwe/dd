apiVersion: v1
kind: Service
metadata:
  name: healthcheck-service
  namespace: api
  labels:
    app: healthcheck
spec:
  selector:
    app: healthcheck
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: ClusterIP
